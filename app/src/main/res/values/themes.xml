<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SimpleIPTV" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Define the AppBarOverlay style -->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowActionBarOverlay">true</item>
    </style>

    <style name="Theme.SimpleIPTV" parent="Base.Theme.SimpleIPTV" />


    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Other theme attributes -->


    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- Customize app bar overlay properties if needed -->
        <!-- For example, you might set background color, elevation, etc. -->
    </style>
</resources>